spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
  application:
    name: ai-auto-poster
  
  datasource:
    url: jdbc:mysql://localhost:3306/testdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Mkoijn@135
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
        
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
    # Disable mail if credentials not provided
    enabled: ${MAIL_ENABLED:false}
            
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

# Scheduler configuration (used by DynamicSchedulerService)
scheduler:
  cron: "0 * * * * *"   # every minute

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:mySecretKey}
  expiration: 86400000

ai:
  openai:
    apiKey: YOUR_API_KEY_HERE
    url: API_URL
  azure:
    apiKey: 21ca2ad4962e432191b93a12ccf14adb
    url: https://devaianakage.openai.azure.com/openai/deployments/Gpt-internal/chat/completions?api-version=2025-01-01-preview
    endpoint: https://devaianakage.openai.azure.com
    dalleDeploymentName: dall-e-3
    apiVersion: 2024-04-01-preview

linkedin:
  client:
    id: ${LINKEDIN_CLIENT_ID:linkedin-client-id}
    secret: ${LINKEDIN_CLIENT_SECRET:linkedin-client-secret}
    redirect-uri: ${LINKEDIN_REDIRECT_URI:http://localhost:8080/api/linkedin/callback}

logging:
  level:
    com.aiautoposter: DEBUG
    org.springframework.security: DEBUG

app:
  upload:
    dir: uploads
  base:
    url: http://localhost:8080

wordpress:
  name: "My Blog"
  url: "https://inventorytoothbrush.s3-tastewp.com/"
  username: "admin"
  app-password: "${WP_PASSWORD:KGDN j2Ct B5qw b74s YZb7 oltO}"
  enabled: true